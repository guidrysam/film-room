/** Shared IFrame API params when Film Room renders its own transport. */
export const YOUTUBE_CHROMELESS_PLAYER_VARS = {
  rel: 0,
  playsinline: 1,
  modestbranding: 1,
  enablejsapi: 1,
  controls: 0,
  disablekb: 1,
  fs: 0,
  iv_load_policy: 3,
} as const;
